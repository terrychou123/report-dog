/**
 * 小規模多機能機構評鑑補充文件定義
 * 115年度臺北市政府社會局小規模多機能機構評鑑基準（45項 + 2加分題）
 *
 * 每個評鑑項目除「檢核表」外，額外提供對應的實務文件範本。
 * 純行為性標準（如「現場觀察」）不產生文件，故不列入。
 * 目前為初版空殼，後續按優先順序逐項填入補充工作頁設計。
 */
import type { SupplementaryDefsMap } from '../supplementary-sheet-types';

export const multiFunctionCareDefs: SupplementaryDefsMap = {
  // 補充工作頁將依 itemId 逐一新增，格式參照 daycare.ts
  // 初版先留空，避免 drift check 誤報（supp 無條目為合法狀態）
};
